import { StyleSheet } from "react-native";
function Styles() {
  return {
    textTimer: {
      color: "#FFF",
      fontSize: 54,
      fontWeight: "bold",
      textAlign: "center",
    },
  };
}

export default StyleSheet.create(Styles());
